
Процедура КнопкаВыполнитьНажатие(Кнопка)
	Если ЗначениеЗаполнено(ТестФизЛицо) Тогда
		ПолучитьДанныеПоФизЛицу(ТестФизЛицо);
	КонецЕсли;
	Если ЗначениеЗаполнено(ТестОрганизация) Тогда
		ПолучитьДанныеПоОрганизации(ТестОрганизация);
	КонецЕсли;
	Если ЗначениеЗаполнено(ТестСотрудник) Тогда
		ПолучитьДанныеПоСотруднику(ТестСотрудник);
	КонецЕсли;
	Если ЗначениеЗаполнено(ТестДолжность) Тогда
		ПолучитьДанныеПоДолжности(ТестДолжность);
	КонецЕсли;
КонецПроцедуры
